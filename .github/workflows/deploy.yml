name: Deploy via SSH

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: SSH Deploy
        uses: appleboy/ssh-action@v1.0.0  # Use fixed version instead of @master
        with:
          host: champagne.hostns.io
          username: ristayco
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22  # Explicitly set default SSH port
          script: |
            echo "Deployment started!"
            # Add your commands here